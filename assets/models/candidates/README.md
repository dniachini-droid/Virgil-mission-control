# Candidate 3D models (inspection only)

Category: **candidate 3D model**. A candidate is accepted only for topology, geometry, UV, material and runtime inspection. It is not a production asset, the application never loads it, and it must not silently redefine the approved character: the concept sheets under `assets/concepts/characters/` are the visual authority.

| File | Character | Origin | Status |
|---|---|---|---|
| `virgil-model-candidate-01.glb` | Virgil | Generated from the approved three-quarter reference with the Stability AI TripoSR demonstration (see the provenance register) | Candidate. Preserved unchanged. Inspection report: `virgil-model-candidate-01.REPORT.md` (bundle record, verbatim) |
| `virgil-model-candidate-02.glb` | Virgil | Generated by the owner with Meshy from a text prompt, not from the approved reference images (see the provenance register; licence basis is the owner's attestation in `docs/decisions/OD-0008-meshy-licence-attestation.md`, verification reads "owner attestation, not verified") | Candidate. Preserved unchanged, byte for byte. No supplied inspection report exists for this file; the measured facts are in `assets/licenses/ASSET_PROVENANCE.md` and summarised below |

Summary of the bundle's inspection of candidate 01: valid binary glTF 2.0, about 4.0 MB, 104,148 position vertices, 207,928 triangles, one mesh and one primitive with about 25 disconnected components, vertex colours present, no UVs, textures, materials, normals, rig, skin or animation; 702 boundary edges, 69 degenerate triangles. Any production copy requires normals, topology repair, logical component separation, optimisation to roughly 20,000 to 40,000 triangles for the first hero test, proper materials, rigging and a fresh row in `assets/licenses/ASSET_PROVENANCE.md`. None of that has been done.

Summary of the measurement of candidate 02, taken from the file itself: valid binary glTF 2.0, 12,391,432 bytes (11.82 MiB), generator `meshy-scene`, declared length equal to actual; 31,156 triangles and 40,179 vertices (15,523 distinct positions) in one mesh, one primitive, one node, primitive mode TRIANGLES; attributes `POSITION`, `NORMAL`, `TANGENT`, `TEXCOORD_0`; one material with base-colour, metallic-roughness and normal textures and `doubleSided: true`; three embedded 2048 × 2048 JPEGs totalling 9.62 MiB against 2.20 MiB of geometry; no extensions, no compression, no external URIs; bounding box 1.341 × 2.000 × 0.498 m centred on the origin, so the pivot is at the model's centre and not at its feet; 3 connected components, 33 boundary edges, 34 non-manifold edges, no degenerate triangles. **Zero animations and zero skins: there is no rig and no clip in this file.** Three consequences are recorded in full in `assets/licenses/ASSET_PROVENANCE.md` — the missing rig, a texture payload that on its own consumes or exceeds the whole slice's transfer budget in `docs/architecture/PERFORMANCE_STRATEGY.md` and 50 % of the mobile tier's texture memory, and backface culling disabled model-wide. None of the three is a licence or provenance problem, none is repaired here, and the file is kept as delivered.
